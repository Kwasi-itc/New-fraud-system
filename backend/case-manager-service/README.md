# Case Manager Service

Standalone case-management service for `New-fraud-system`.

It owns cases, inboxes, tags, case events, files metadata, workflow action intake, and screening-facing case events in the `case_manager` PostgreSQL schema.
