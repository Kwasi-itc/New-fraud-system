# Ingestion Service Documentation

This directory mirrors the grouped endpoint documentation style used in `data-model-service`.

Current endpoint-group docs:

- [Synchronous Ingest And Record Read](./synchronous-ingest-and-record-read.md)
- [CSV Upload Logs And Worker Flow](./csv-upload-logs-and-worker.md)

These docs are intended to explain:

- what each endpoint group does today
- the current contract shape
- the service and handler files behind the routes
- the design intent behind the extraction
